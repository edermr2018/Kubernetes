apiVersion: v1
kind: ServiceAccount
metadata:
  name: admin-user # Permite crear un service account para ver en un dashboard la informacion que se tiene en los pods, ns, 
  namespace: kubernetes-dashboard